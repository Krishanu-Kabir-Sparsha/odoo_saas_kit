<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ════════════════════════════════════════════════════════════════
         Reusable pricing block: package cards + "Customize Your Plan"
         calculator + module modals + its CSS/JS. It fetches its own data, so
         it can be embedded on ANY page with:  <t t-call="saas_portal.pricing_block"/>
         (e.g. dropped into the website homepage via the HTML/CSS editor).
         ════════════════════════════════════════════════════════════════ -->
    <template id="pricing_block" name="SaaS Pricing Block">
        <t t-set="packages" t-value="request.env['saas.package'].sudo().search([('active', '=', True)])"/>
        <t t-set="currency_symbol" t-value="request.env.company.currency_id.symbol or '৳'"/>
        <t t-set="duration_data" t-value="packages.get_pricing_duration_data()"/>
            <div class="saas-pricing-page">
                <!-- Hero Section -->
                <div class="pricing-hero">
                    <div class="container text-center">
                        <h1 class="pricing-title">Plans &amp; Pricing</h1>
                        <p class="pricing-subtitle">Choose the perfect plan to power your business. Scale anytime.</p>

                        <!-- Billing Toggle -->
                        <div class="billing-toggle-wrap mt-4">
                            <div class="billing-toggle" id="billingToggle">
                                <button type="button" class="toggle-btn active" data-cycle="monthly">Monthly</button>
                                <button type="button" class="toggle-btn" data-cycle="yearly">Yearly</button>
                                <div class="toggle-pill"></div>
                            </div>
                            <span class="save-tag">Save 15%</span>
                        </div>
                    </div>
                </div>

                <!-- Package Cards -->
                <div class="container">
                    <div class="pricing-grid" id="packages-container">
                        <t t-foreach="packages" t-as="package">
                            <div t-attf-class="pricing-card #{package.is_popular and 'popular' or ''}">
                                <!-- Popular Badge -->
                                <t t-if="package.is_popular">
                                    <div class="popular-badge">
                                        <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
                                        Most Popular
                                    </div>
                                </t>

                                <!-- Card Header -->
                                <div class="card-header-section">
                                    <h3 class="plan-name"><t t-esc="package.name"/></h3>
                                    <p class="plan-desc" t-if="package.description">
                                        <t t-esc="package.description[:80] if package.description else ''"/>
                                    </p>
                                </div>

                                <!-- Pricing -->
                                <div class="price-section">
                                    <div class="price-display monthly-price">
                                        <span class="currency-sym"><t t-esc="request.env.company.currency_id.symbol"/></span>
                                        <span class="price-val"><t t-esc="'{:,.0f}'.format(package.monthly_price)"/></span>
                                        <span class="price-per">/month</span>
                                    </div>
                                    <div class="price-display yearly-price" style="display:none;">
                                        <span class="currency-sym"><t t-esc="request.env.company.currency_id.symbol"/></span>
                                        <span class="price-val"><t t-esc="'{:,.0f}'.format(package.yearly_price)"/></span>
                                        <span class="price-per">/year</span>
                                    </div>
                                    <t t-if="package.setup_fee">
                                        <span class="setup-fee">+ <t t-esc="'{:,.0f}'.format(package.setup_fee)"/> setup fee</span>
                                    </t>
                                </div>

                                <!-- CTA Button -->
                                <a t-attf-href="/saas/signup?package_id={{package.id}}"
                                   t-attf-class="cta-button #{package.is_popular and 'cta-primary' or 'cta-outline'}">
                                    Get Started
                                </a>

                                <!-- Divider -->
                                <div class="features-divider">
                                    <span>What's included</span>
                                </div>

                                <!-- General Description (if set by admin) -->
                                <t t-if="package.included_description">
                                    <p class="included-description"><t t-esc="package.included_description"/></p>
                                </t>

                                <!-- Features List (collapsible descriptions, max 10 shown) -->
                                <div class="features-list">
                                    <t t-foreach="package.feature_ids[:10]" t-as="feature">
                                        <div class="feature-item">
                                            <div class="feature-check">
                                                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                                            </div>
                                            <div class="feature-content">
                                                <div t-attf-class="feature-header #{feature.description and 'has-desc' or ''}"
                                                     t-if="feature.description"
                                                     onclick="this.parentElement.classList.toggle('open')">
                                                    <span class="feature-module-name"><t t-esc="feature.name"/></span>
                                                    <svg class="chevron-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
                                                </div>
                                                <div class="feature-header" t-if="not feature.description">
                                                    <span class="feature-module-name"><t t-esc="feature.name"/></span>
                                                </div>
                                                <t t-if="feature.description">
                                                    <div class="feature-desc-wrap">
                                                        <p class="feature-description"><t t-esc="feature.description"/></p>
                                                    </div>
                                                </t>
                                            </div>
                                        </div>
                                    </t>
                                    <!-- View All button when more than 10 features exist -->
                                    <t t-if="len(package.feature_ids) > 10">
                                        <button type="button" class="view-all-btn"
                                                t-att-data-package-id="package.id"
                                                onclick="saasOpenModuleModal(this)">
                                            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
                                            View all <t t-esc="len(package.feature_ids)"/> modules
                                        </button>
                                    </t>
                                    <!-- Module count / custom footer text -->
                                    <t t-if="package.feature_ids">
                                        <div class="module-summary">
                                            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/><polyline points="3.27 6.96 12 12.01 20.73 6.96"/><line x1="12" y1="22.08" x2="12" y2="12"/></svg>
                                            <span>
                                                <t t-if="package.card_footer_text"><t t-esc="package.card_footer_text"/></t>
                                                <t t-else=""><t t-esc="len(package.feature_ids)"/> modules included</t>
                                            </span>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </t>
                    </div>
                </div>


                <!-- ════════════════════════════════════════════════════
                     CUSTOMIZE YOUR PLAN SECTION
                     ════════════════════════════════════════════════════ -->
                <div class="customize-section" id="customizePlan">
                    <div class="container">
                        <div class="text-center mb-4">
                            <h2 class="customize-title">Customize Your Plan</h2>
                            <p class="customize-subtitle">Choose your package and see real-time pricing with volume discounts</p>
                        </div>

                        <div class="customize-grid">
                            <!-- LEFT: Selectors -->
                            <div class="customize-left">
                                <!-- Tier Selection -->
                                <div class="selector-group">
                                    <h4 class="selector-label">Select Tier</h4>
                                    <div class="tier-buttons" id="tierButtons">
                                        <t t-foreach="packages" t-as="package">
                                            <button type="button"
                                                    class="tier-btn"
                                                    t-att-data-package-id="package.id"
                                                    t-att-data-package-name="package.name"
                                                    t-att-data-monthly-price="package.monthly_price">
                                                <t t-esc="package.name"/>
                                            </button>
                                        </t>
                                    </div>
                                </div>

                                <!-- Duration Selection -->
                                <div class="selector-group">
                                    <h4 class="selector-label">Select Duration</h4>
                                    <div class="duration-buttons" id="durationButtons">
                                        <!-- Populated dynamically by JS -->
                                    </div>
                                </div>

                                <!-- Included Modules -->
                                <div class="selector-group" id="modulesSection" style="display:none;">
                                    <h4 class="selector-label">Included Modules</h4>
                                    <div class="modules-grid" id="modulesGrid">
                                        <!-- Populated dynamically by JS -->
                                    </div>
                                </div>
                            </div>

                            <!-- RIGHT: Price Breakdown -->
                            <div class="customize-right">
                                <div class="breakdown-card" id="breakdownCard" style="display:none;">
                                    <h4 class="breakdown-title">Price Breakdown</h4>

                                    <div class="breakdown-rows">
                                        <div class="breakdown-row">
                                            <span class="breakdown-label" id="basePriceLabel">Base Price</span>
                                            <span class="breakdown-value" id="basePriceValue">-</span>
                                        </div>
                                        <div class="breakdown-row discount-row" id="discountRow">
                                            <span class="breakdown-label" id="discountLabel">Duration Discount</span>
                                            <span class="breakdown-value discount-value" id="discountValue">-</span>
                                        </div>
                                        <div class="breakdown-row">
                                            <span class="breakdown-label">Monthly Price</span>
                                            <span class="breakdown-value" id="monthlyPriceValue">-</span>
                                        </div>
                                        <div class="breakdown-row">
                                            <span class="breakdown-label">Duration</span>
                                            <span class="breakdown-value" id="durationValue">-</span>
                                        </div>
                                        <t t-if="True">
                                            <div class="breakdown-row setup-row" id="setupRow" style="display:none;">
                                                <span class="breakdown-label">Setup Fee</span>
                                                <span class="breakdown-value" id="setupFeeValue">-</span>
                                            </div>
                                        </t>
                                    </div>

                                    <div class="breakdown-total">
                                        <div class="total-row">
                                            <span class="total-label">Total Price</span>
                                            <span class="total-value" id="totalPriceValue">-</span>
                                        </div>
                                        <span class="total-billed" id="totalBilled">-</span>
                                    </div>

                                    <a href="#" class="customize-cta" id="customizeCta">
                                        Get Started Now
                                    </a>
                                    <p class="customize-guarantee">30-day money-back guarantee • Cancel anytime</p>
                                </div>

                                <!-- Placeholder when nothing selected -->
                                <div class="breakdown-placeholder" id="breakdownPlaceholder">
                                    <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="#c7cad4" stroke-width="1.5"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/><polyline points="3.27 6.96 12 12.01 20.73 6.96"/><line x1="12" y1="22.08" x2="12" y2="12"/></svg>
                                    <p>Select a tier to see pricing</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Bottom CTA -->
                <div class="container text-center mt-4 mb-5 pb-5">
                    <p style="font-size:0.92rem;color:#8f96a8;">
                        Need a custom plan? <a href="/contactus" style="color:#6366f1;font-weight:600;">Contact our sales team</a>
                    </p>
                </div>

                <!-- Module Modals (one per package that has more than 10 features) -->
                <t t-foreach="packages" t-as="pkg_modal">
                    <t t-if="len(pkg_modal.feature_ids) > 10">
                        <div t-attf-id="saasModuleModal_{{pkg_modal.id}}"
                             class="saas-module-modal" style="display:none;" role="dialog" aria-modal="true">
                            <div class="saas-modal-overlay" onclick="saasCloseModuleModal(this)"></div>
                            <div class="saas-modal-box">
                                <div class="saas-modal-header">
                                    <div>
                                        <h4 class="saas-modal-title"><t t-esc="pkg_modal.name"/></h4>
                                        <p class="saas-modal-subtitle"><t t-esc="len(pkg_modal.feature_ids)"/> modules included</p>
                                    </div>
                                    <button type="button" class="saas-modal-close"
                                            onclick="saasCloseModuleModal(this)" aria-label="Close">
                                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                                    </button>
                                </div>
                                <div class="saas-modal-body">
                                    <t t-foreach="pkg_modal.feature_ids" t-as="mfeature">
                                        <div class="feature-item">
                                            <div class="feature-check">
                                                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                                            </div>
                                            <div class="feature-content">
                                                <div t-attf-class="feature-header #{mfeature.description and 'has-desc' or ''}"
                                                     t-if="mfeature.description"
                                                     onclick="this.parentElement.classList.toggle('open')">
                                                    <span class="feature-module-name"><t t-esc="mfeature.name"/></span>
                                                    <svg class="chevron-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
                                                </div>
                                                <div class="feature-header" t-if="not mfeature.description">
                                                    <span class="feature-module-name"><t t-esc="mfeature.name"/></span>
                                                </div>
                                                <t t-if="mfeature.description">
                                                    <div class="feature-desc-wrap">
                                                        <p class="feature-description"><t t-esc="mfeature.description"/></p>
                                                    </div>
                                                </t>
                                            </div>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </div>
                    </t>
                </t>
            </div>

            <style>
                /* ═══════════════════════════════════════
                   DESIGN TOKENS
                   ═══════════════════════════════════════ */
                .saas-pricing-page {
                    --bg-page: #f7f8fc;
                    --bg-card: #ffffff;
                    --border-card: #e8ecf1;
                    --border-popular: #6366f1;
                    --txt-dark: #1a1d2e;
                    --txt-mid: #5f6980;
                    --txt-muted: #8f96a8;
                    --accent: #6366f1;
                    --accent-light: #eef2ff;
                    --accent-grad: linear-gradient(135deg, #6366f1, #8b5cf6);
                    --green: #22c55e;
                    --green-bg: #f0fdf4;
                    --shadow-card: 0 1px 3px rgba(0,0,0,0.04), 0 4px 12px rgba(0,0,0,0.03);
                    --shadow-hover: 0 8px 30px rgba(0,0,0,0.08);
                    --shadow-popular: 0 8px 40px rgba(99,102,241,0.18);
                    --r-card: 16px;
                    --r-btn: 12px;
                    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
                    background: var(--bg-page);
                    min-height: 100vh;
                }

                /* ═══════ HERO ═══════ */
                .pricing-hero {
                    padding: 56px 0 36px;
                    background: linear-gradient(180deg, #eff0ff 0%, var(--bg-page) 100%);
                }
                .pricing-title {
                    font-size: 2.6rem;
                    font-weight: 800;
                    color: var(--txt-dark);
                    letter-spacing: -0.03em;
                    margin-bottom: 10px;
                }
                .pricing-subtitle {
                    font-size: 1.1rem;
                    color: var(--txt-mid);
                    max-width: 460px;
                    margin: 0 auto;
                    line-height: 1.6;
                }

                /* ═══════ BILLING TOGGLE ═══════ */
                .billing-toggle-wrap {
                    display: inline-flex;
                    align-items: center;
                    gap: 10px;
                }
                .billing-toggle {
                    position: relative;
                    display: inline-flex;
                    background: #e4e7ee;
                    border-radius: 40px;
                    padding: 4px;
                }
                .toggle-btn {
                    position: relative;
                    z-index: 2;
                    border: none;
                    background: none;
                    padding: 9px 22px;
                    font-size: 0.88rem;
                    font-weight: 600;
                    color: var(--txt-mid);
                    cursor: pointer;
                    border-radius: 40px;
                    transition: color 0.3s;
                    line-height: 1;
                }
                .toggle-btn.active { color: #fff; }
                .toggle-pill {
                    position: absolute;
                    top: 4px;
                    left: 4px;
                    height: calc(100% - 8px);
                    border-radius: 40px;
                    background: var(--accent-grad);
                    box-shadow: 0 2px 8px rgba(99,102,241,0.3);
                    transition: transform 0.35s cubic-bezier(0.4,0,0.2,1), width 0.35s cubic-bezier(0.4,0,0.2,1);
                    z-index: 1;
                }
                .save-tag {
                    display: inline-block;
                    background: rgba(34,197,94,0.12);
                    color: #16a34a;
                    font-size: 0.72rem;
                    font-weight: 700;
                    padding: 4px 10px;
                    border-radius: 12px;
                    letter-spacing: 0.01em;
                }

                /* ═══════ PRICING GRID ═══════ */
                .pricing-grid {
                    display: grid;
                    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                    gap: 24px;
                    max-width: 1100px;
                    margin: 0 auto;
                    padding: 10px 0 50px;
                    align-items: stretch;
                }

                /* ═══════ PRICING CARD ═══════ */
                .pricing-card {
                    position: relative;
                    background: var(--bg-card);
                    border: 1.5px solid var(--border-card);
                    border-radius: var(--r-card);
                    padding: 34px 28px 28px;
                    box-shadow: var(--shadow-card);
                    transition: transform 0.3s, box-shadow 0.3s;
                    display: flex;
                    flex-direction: column;
                }
                .pricing-card:hover {
                    transform: translateY(-4px);
                    box-shadow: var(--shadow-hover);
                }
                .pricing-card.popular {
                    border-color: var(--border-popular);
                    box-shadow: var(--shadow-popular);
                }
                .pricing-card.popular:hover {
                    box-shadow: var(--shadow-popular), var(--shadow-hover);
                }

                /* Popular Badge */
                .popular-badge {
                    position: absolute;
                    top: -12px;
                    left: 50%;
                    transform: translateX(-50%);
                    background: var(--accent-grad);
                    color: #fff;
                    font-size: 0.72rem;
                    font-weight: 700;
                    padding: 5px 14px;
                    border-radius: 20px;
                    display: flex;
                    align-items: center;
                    gap: 5px;
                    box-shadow: 0 3px 12px rgba(99,102,241,0.35);
                    white-space: nowrap;
                }

                /* Card Header */
                .card-header-section { margin-bottom: 16px; }
                .plan-name {
                    font-size: 1.3rem;
                    font-weight: 700;
                    color: var(--txt-dark);
                    margin-bottom: 4px;
                }
                .plan-desc {
                    font-size: 0.85rem;
                    font-weight: 600;
                    color: var(--txt-mid);
                    line-height: 1.5;
                    margin: 0;
                }

                /* Price */
                .price-section { margin-bottom: 22px; }
                .price-display { display: flex; align-items: baseline; gap: 1px; }
                .currency-sym {
                    font-size: 1.3rem;
                    font-weight: 700;
                    color: var(--txt-dark);
                    align-self: flex-start;
                    margin-top: 6px;
                }
                .price-val {
                    font-size: 2.8rem;
                    font-weight: 800;
                    color: var(--txt-dark);
                    letter-spacing: -0.03em;
                    line-height: 1;
                }
                .price-per {
                    font-size: 0.92rem;
                    color: var(--txt-muted);
                    font-weight: 500;
                    margin-left: 3px;
                }
                .setup-fee {
                    display: block;
                    font-size: 0.78rem;
                    color: var(--txt-muted);
                    margin-top: 4px;
                }

                /* CTA */
                .cta-button {
                    display: block;
                    width: 100%;
                    text-align: center;
                    padding: 13px 20px;
                    border-radius: var(--r-btn);
                    font-size: 0.92rem;
                    font-weight: 700;
                    text-decoration: none !important;
                    transition: all 0.25s;
                    margin-bottom: 22px;
                }
                .cta-outline {
                    background: transparent;
                    border: 2px solid var(--border-card);
                    color: var(--txt-dark);
                }
                .cta-outline:hover {
                    background: var(--accent-light);
                    border-color: var(--accent);
                    color: var(--accent);
                }
                .cta-primary {
                    background: var(--accent-grad);
                    border: 2px solid transparent;
                    color: #fff;
                    box-shadow: 0 4px 14px rgba(99,102,241,0.3);
                }
                .cta-primary:hover {
                    box-shadow: 0 6px 20px rgba(99,102,241,0.4);
                    color: #fff;
                }

                /* Divider */
                .features-divider {
                    display: flex;
                    align-items: center;
                    margin-bottom: 18px;
                }
                .features-divider span {
                    font-size: 0.7rem;
                    font-weight: 700;
                    text-transform: uppercase;
                    letter-spacing: 0.08em;
                    color: var(--txt-muted);
                    white-space: nowrap;
                    padding-right: 12px;
                }
                .features-divider::after {
                    content: '';
                    flex: 1;
                    height: 1px;
                    background: var(--border-card);
                }

                /* General Description under "What's Included" */
                .included-description {
                    font-size: 0.82rem;
                    color: var(--txt-mid);
                    line-height: 1.55;
                    margin: 0 0 10px 0;
                    padding: 0 2px;
                }

                /* ═══════ FEATURES (COLLAPSIBLE) ═══════ */
                .features-list {
                    flex: 1;
                    display: flex;
                    flex-direction: column;
                    gap: 6px;
                }
                .feature-item {
                    display: flex;
                    align-items: flex-start;
                    gap: 10px;
                }
                .feature-check {
                    flex-shrink: 0;
                    width: 22px;
                    height: 22px;
                    background: var(--green-bg);
                    border-radius: 50%;
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    margin-top: 7px;
                }
                .feature-check svg { stroke: var(--green); }

                .feature-content {
                    flex: 1;
                    min-width: 0;
                }
                .feature-header {
                    display: flex;
                    align-items: center;
                    justify-content: space-between;
                    padding: 6px 0;
                }
                .feature-header.has-desc {
                    cursor: pointer;
                    border-radius: 6px;
                    padding: 6px 4px;
                    margin: 0 -4px;
                    transition: background 0.2s;
                }
                .feature-header.has-desc:hover {
                    background: #f4f5f9;
                }
                .feature-module-name {
                    font-size: 0.88rem;
                    font-weight: 600;
                    color: var(--txt-dark);
                    line-height: 1.4;
                }

                /* Chevron Arrow */
                .chevron-icon {
                    flex-shrink: 0;
                    stroke: var(--txt-muted);
                    transition: transform 0.3s ease;
                    margin-left: 6px;
                }
                .feature-content.open .chevron-icon {
                    transform: rotate(180deg);
                }

                /* Collapsible Description */
                .feature-desc-wrap {
                    max-height: 0;
                    overflow: hidden;
                    transition: max-height 0.35s ease, opacity 0.3s ease;
                    opacity: 0;
                }
                .feature-content.open .feature-desc-wrap {
                    max-height: 200px;
                    opacity: 1;
                }
                .feature-description {
                    font-size: 0.8rem;
                    color: var(--txt-muted);
                    line-height: 1.5;
                    margin: 0;
                    padding: 2px 4px 8px;
                }

                /* Module Summary */
                .module-summary {
                    display: flex;
                    align-items: center;
                    gap: 8px;
                    padding-top: 14px;
                    margin-top: auto;
                    border-top: 1px dashed var(--border-card);
                    font-size: 0.83rem;
                    font-weight: 600;
                    color: var(--accent);
                }
                .module-summary svg { stroke: var(--accent); flex-shrink: 0; }

                /* View All Button */
                .view-all-btn {
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    gap: 7px;
                    width: 100%;
                    padding: 10px 14px;
                    border: 1.5px dashed var(--accent);
                    border-radius: 10px;
                    background: var(--accent-light);
                    color: var(--accent);
                    font-size: 0.82rem;
                    font-weight: 600;
                    cursor: pointer;
                    transition: all 0.2s;
                    margin-top: 6px;
                }
                .view-all-btn:hover { background: #e0e7ff; border-style: solid; }
                .view-all-btn svg { stroke: var(--accent); flex-shrink: 0; }

                /* ═══════ MODULE MODAL ═══════ */
                .saas-module-modal {
                    position: fixed;
                    inset: 0;
                    z-index: 9999;
                    display: flex;
                    align-items: center;
                    justify-content: center;
                }
                .saas-modal-overlay {
                    position: absolute;
                    inset: 0;
                    background: rgba(26,29,46,0.55);
                    backdrop-filter: blur(3px);
                    cursor: pointer;
                }
                .saas-modal-box {
                    position: relative;
                    background: #fff;
                    border-radius: var(--r-card);
                    padding: 28px;
                    max-width: 580px;
                    width: 92%;
                    max-height: 80vh;
                    overflow-y: auto;
                    box-shadow: 0 24px 60px rgba(0,0,0,0.22);
                    animation: saasModalIn 0.22s ease;
                }
                @keyframes saasModalIn {
                    from { opacity: 0; transform: translateY(16px) scale(0.97); }
                    to   { opacity: 1; transform: translateY(0) scale(1); }
                }
                .saas-modal-header {
                    display: flex;
                    justify-content: space-between;
                    align-items: flex-start;
                    margin-bottom: 20px;
                    padding-bottom: 16px;
                    border-bottom: 1px solid var(--border-card);
                }
                .saas-modal-title {
                    font-size: 1.15rem;
                    font-weight: 700;
                    color: var(--txt-dark);
                    margin: 0 0 3px;
                }
                .saas-modal-subtitle {
                    font-size: 0.8rem;
                    color: var(--txt-muted);
                    margin: 0;
                }
                .saas-modal-close {
                    flex-shrink: 0;
                    border: none;
                    background: none;
                    cursor: pointer;
                    color: var(--txt-muted);
                    padding: 4px 6px;
                    border-radius: 8px;
                    transition: background 0.2s, color 0.2s;
                    margin-left: 12px;
                    display: flex;
                    align-items: center;
                }
                .saas-modal-close:hover { background: #f4f5f9; color: var(--txt-dark); }
                .saas-modal-body {
                    display: flex;
                    flex-direction: column;
                    gap: 6px;
                }

                /* ═══════ RESPONSIVE ═══════ */
                @media (max-width: 768px) {
                    .pricing-title { font-size: 2rem; }
                    .pricing-grid {
                        grid-template-columns: 1fr;
                        max-width: 420px;
                        padding: 16px 12px 40px;
                    }
                    .pricing-card { padding: 28px 22px 22px; }
                    .price-val { font-size: 2.2rem; }
                    .customize-grid { grid-template-columns: 1fr; }
                    .customize-right { position: static; }
                }

                /* ═══════════════════════════════════════
                   CUSTOMIZE YOUR PLAN SECTION
                   ═══════════════════════════════════════ */
                .customize-section {
                    padding: 60px 0 20px;
                }
                .customize-title {
                    font-size: 2.2rem;
                    font-weight: 800;
                    color: var(--txt-dark);
                    letter-spacing: -0.02em;
                }
                .customize-subtitle {
                    font-size: 1rem;
                    color: var(--txt-mid);
                    margin-top: 6px;
                }
                .customize-grid {
                    display: grid;
                    grid-template-columns: 1fr 1fr;
                    gap: 28px;
                    max-width: 960px;
                    margin: 0 auto;
                    align-items: start;
                }
                .customize-left {
                    background: var(--bg-card);
                    border: 1.5px solid var(--border-card);
                    border-radius: var(--r-card);
                    padding: 28px 24px;
                    box-shadow: var(--shadow-card);
                    max-height: 75vh;
                    overflow-y: auto;
                }
                /* Polished scrollbar for the customize card */
                .customize-left::-webkit-scrollbar {
                    width: 6px;
                }
                .customize-left::-webkit-scrollbar-track {
                    background: transparent;
                }
                .customize-left::-webkit-scrollbar-thumb {
                    background: var(--border-card);
                    border-radius: 3px;
                }
                .customize-left::-webkit-scrollbar-thumb:hover {
                    background: var(--txt-muted);
                }
                .customize-right {
                    position: sticky;
                    top: 80px;
                }

                /* Selector Groups */
                .selector-group { margin-bottom: 24px; }
                .selector-group:last-child { margin-bottom: 0; }
                .selector-label {
                    font-size: 0.85rem;
                    font-weight: 700;
                    color: var(--txt-dark);
                    margin-bottom: 10px;
                }

                /* Tier Buttons */
                .tier-buttons {
                    display: flex;
                    flex-wrap: wrap;
                    gap: 8px;
                }
                .tier-btn {
                    position: relative;
                    padding: 10px 20px;
                    border: 1.5px solid var(--border-card);
                    border-radius: 10px;
                    background: #fff;
                    font-size: 0.85rem;
                    font-weight: 600;
                    color: var(--txt-dark);
                    cursor: pointer;
                    transition: all 0.2s;
                    min-width: 110px;
                    text-align: center;
                }
                .tier-btn:hover {
                    border-color: var(--accent);
                    background: var(--accent-light);
                }
                .tier-btn.active {
                    background: var(--accent-grad);
                    color: #fff;
                    border-color: transparent;
                    box-shadow: 0 3px 12px rgba(99,102,241,0.3);
                }

                /* Duration Buttons */
                .duration-buttons {
                    display: flex;
                    flex-wrap: wrap;
                    gap: 8px;
                }
                .dur-btn {
                    position: relative;
                    padding: 10px 20px;
                    border: 1.5px solid var(--border-card);
                    border-radius: 10px;
                    background: #fff;
                    font-size: 0.85rem;
                    font-weight: 600;
                    color: var(--txt-dark);
                    cursor: pointer;
                    transition: all 0.2s;
                    min-width: 110px;
                    text-align: center;
                }
                .dur-btn:hover {
                    border-color: var(--accent);
                    background: var(--accent-light);
                }
                .dur-btn.active {
                    background: var(--accent-grad);
                    color: #fff;
                    border-color: transparent;
                    box-shadow: 0 3px 12px rgba(99,102,241,0.3);
                }
                .discount-badge {
                    position: absolute;
                    top: -8px;
                    right: -6px;
                    background: #ef4444;
                    color: #fff;
                    font-size: 0.62rem;
                    font-weight: 700;
                    padding: 2px 6px;
                    border-radius: 8px;
                    line-height: 1.2;
                }

                /* Modules Grid */
                .modules-grid {
                    display: grid;
                    grid-template-columns: 1fr 1fr;
                    gap: 6px;
                }
                .module-chip {
                    display: flex;
                    align-items: center;
                    gap: 6px;
                    padding: 8px 10px;
                    background: #f7f8fc;
                    border: 1px solid var(--border-card);
                    border-radius: 8px;
                    font-size: 0.78rem;
                    font-weight: 500;
                    color: var(--txt-dark);
                    line-height: 1.3;
                }
                .module-chip svg {
                    flex-shrink: 0;
                    stroke: var(--green);
                }

                /* Breakdown Card */
                .breakdown-card {
                    background: var(--bg-card);
                    border: 1.5px solid var(--border-card);
                    border-radius: var(--r-card);
                    padding: 28px 24px;
                    box-shadow: var(--shadow-card);
                }
                .breakdown-title {
                    font-size: 1.2rem;
                    font-weight: 700;
                    color: var(--txt-dark);
                    margin-bottom: 20px;
                }
                .breakdown-rows {
                    border-bottom: 1px solid var(--border-card);
                    padding-bottom: 16px;
                    margin-bottom: 16px;
                }
                .breakdown-row {
                    display: flex;
                    justify-content: space-between;
                    align-items: center;
                    padding: 8px 0;
                }
                .breakdown-label {
                    font-size: 0.88rem;
                    color: var(--txt-mid);
                }
                .breakdown-value {
                    font-size: 0.88rem;
                    font-weight: 600;
                    color: var(--txt-dark);
                    text-align: right;
                }
                .discount-value {
                    color: #16a34a;
                }
                .total-row {
                    display: flex;
                    justify-content: space-between;
                    align-items: center;
                    margin-bottom: 4px;
                }
                .total-label {
                    font-size: 1rem;
                    font-weight: 700;
                    color: var(--txt-dark);
                }
                .total-value {
                    font-size: 1.5rem;
                    font-weight: 800;
                    color: var(--txt-dark);
                }
                .total-billed {
                    display: block;
                    font-size: 0.78rem;
                    color: var(--txt-muted);
                    text-align: right;
                }
                .customize-cta {
                    display: block;
                    width: 100%;
                    text-align: center;
                    padding: 14px;
                    border-radius: var(--r-btn);
                    background: var(--accent-grad);
                    color: #fff;
                    font-size: 0.95rem;
                    font-weight: 700;
                    text-decoration: none !important;
                    margin-top: 20px;
                    box-shadow: 0 4px 14px rgba(99,102,241,0.3);
                    transition: box-shadow 0.25s, transform 0.25s;
                }
                .customize-cta:hover {
                    box-shadow: 0 6px 20px rgba(99,102,241,0.4);
                    transform: translateY(-1px);
                    color: #fff;
                }
                .customize-guarantee {
                    text-align: center;
                    font-size: 0.75rem;
                    color: var(--txt-muted);
                    margin-top: 12px;
                    margin-bottom: 0;
                }

                /* Placeholder */
                .breakdown-placeholder {
                    background: var(--bg-card);
                    border: 1.5px dashed var(--border-card);
                    border-radius: var(--r-card);
                    padding: 60px 24px;
                    text-align: center;
                    color: var(--txt-muted);
                }
                .breakdown-placeholder p {
                    margin-top: 12px;
                    font-size: 0.9rem;
                }
            </style>

            <script type="application/json" id="durationDataJson" t-att-data-currency="currency_symbol"><t t-out="duration_data"/></script>
            <script src="/saas_portal/static/src/js/pricing_page.js"></script>
    </template>

    <!-- Public /saas/packages page: website chrome + error slot + the block. -->
    <template id="package_listing" name="SaaS Packages">
        <t t-call="website.layout">
            <t t-if="error">
                <div class="container" style="padding-top:24px;">
                    <div class="alert alert-danger mx-auto" style="max-width:600px;">
                        <t t-esc="error"/>
                    </div>
                </div>
            </t>
            <t t-call="saas_portal.pricing_block"/>
        </t>
    </template>

    <!-- Signup Form -->
    <template id="signup_form" name="Signup Form">
        <t t-call="website.layout">
            <div class="container mt-4 mb-5">
                <div class="row justify-content-center">
                    <div class="col-md-6">
                        <div class="card shadow-sm border-0">
                            <div class="card-header bg-white border-bottom py-3">
                                <h3 class="mb-1 fw-bold">Get Started</h3>
                                <small class="text-muted">
                                    Create your account to subscribe to
                                    <strong><t t-esc="package.name if package else ''"/></strong>
                                </small>
                            </div>
                            <div class="card-body p-4">
                                <t t-if="error">
                                    <div class="alert alert-danger"><t t-esc="error"/></div>
                                </t>

                                <!-- Login prompt for existing users -->
                                <div class="alert alert-info d-flex align-items-center mb-4">
                                    <i class="fa fa-info-circle me-2 fs-5"/>
                                    <div>
                                        Already have an account?
                                        <a t-attf-href="/web/login?redirect=/saas/signup%3Fpackage_id%3D{{ package.id }}%26duration%3D{{ duration }}"
                                           class="fw-semibold">
                                            Log in here
                                        </a>
                                        to proceed directly to checkout.
                                    </div>
                                </div>

                                <form method="post">
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                    <input type="hidden" name="package_id" t-att-value="package.id if package else ''"/>
                                    <input type="hidden" name="billing_cycle" t-att-value="billing_cycle"/>
                                    <input type="hidden" name="duration" t-att-value="duration"/>

                                    <div class="mb-3">
                                        <label class="form-label fw-semibold">Full Name *</label>
                                        <input type="text" name="name" class="form-control" required="required"
                                               placeholder="e.g. John Doe"/>
                                    </div>
                                    <div class="mb-3">
                                        <label class="form-label fw-semibold">Email Address *</label>
                                        <input type="email" name="email" class="form-control" required="required"
                                               placeholder="you@company.com"/>
                                    </div>
                                    <div class="row">
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label fw-semibold">Password *</label>
                                            <input type="password" name="password" class="form-control"
                                                   required="required" minlength="8"/>
                                            <small class="form-text text-muted">Minimum 8 characters</small>
                                        </div>
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label fw-semibold">Confirm Password *</label>
                                            <input type="password" name="confirm_password" class="form-control"
                                                   required="required"/>
                                        </div>
                                    </div>
                                    <div class="row">
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label fw-semibold">Company Name</label>
                                            <input type="text" name="company_name" class="form-control"
                                                   placeholder="Optional"/>
                                        </div>
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label fw-semibold">Phone Number</label>
                                            <input type="tel" name="phone" class="form-control"
                                                   placeholder="Optional"/>
                                        </div>
                                    </div>
                                    <button type="submit"
                                            class="btn btn-primary btn-lg w-100 rounded-pill fw-semibold mt-2"
                                            style="background: linear-gradient(135deg, #7c3aed, #3b82f6); border: none;">
                                        Continue to Checkout
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>
</odoo>
